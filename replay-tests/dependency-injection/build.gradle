dependencies {
  compile('com.codeborne.replay:replay-guice:1.6-SNAPSHOT')
}

repositories {
  ivy {
    url uri('https://repo.codeborne.com')
    layout "pattern", {
      artifact "/com.google.inject/[module]/[module]-[revision].[ext]"
    }
  }
}
